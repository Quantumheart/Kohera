import 'dart:typed_data';

/// Web stub: [FilePicker.saveFile] already downloads the bytes when a path is
/// returned, so no file write is needed.
Future<void> writeExportFile(String path, Uint8List bytes) async {}
